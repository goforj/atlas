Add an App readiness hook that verifies the existing invoice application behavior before startup completes. Register it on `runtime.BeforeStartup` through the existing lifecycle registry, pass the startup context through that boundary, and let a readiness failure stop lifecycle startup with useful failure context. Do not rely on a reserved fixture ID: use a valid readiness check appropriate to the existing invoice boundary. Keep I/O out of constructors and runtime bootstrap. The existing invoice service and repository are already the boundary this hook needs; do not change them as part of this lifecycle task.
