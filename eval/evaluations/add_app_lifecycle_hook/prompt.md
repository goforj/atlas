Add an App readiness hook in `app/lifecycle.go` using `NewLifecycleRegistry(*invoices.Service)`, `Register(*runtime.Lifecycle)`, and `BeforeStartup(ctx)`. Register `BeforeStartup` for `runtime.BeforeStartup`, pass the startup context to the existing invoice service, and perform a valid lookup with a nonempty invoice ID. Return readiness errors so they stop lifecycle startup without losing the underlying cause. Keep I/O in the hook rather than constructors or runtime bootstrap, and do not change the existing invoice service or repository for this task.
