Add an account transfer workflow in `internal/accounts/repository.go` and `internal/accounts/service.go` with `Account` fields `ID` and `BalanceCents`. Provide a database-backed `Repository` with context-aware `WithTransaction` and `AdjustBalance` methods, and a `Transfer(ctx, fromID, toID string, amountCents int64) error` method on `Service`, built with `NewService(newRepository(db))`. A positive transfer must debit one account and credit the other in the same transaction. Reject zero and negative amounts, roll back both changes when either account cannot be updated, propagate cancellation, and prove commit and rollback through durable state. Register the repository and service with the App's existing Wire sets.
