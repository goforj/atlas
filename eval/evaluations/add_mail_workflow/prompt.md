Add `ReceiptMailer` in `internal/invoices/receipt_mailer.go`, constructed by `NewReceiptMailer(NewService(repository), *mail.Manager)`, with `Send(ctx, invoiceID, recipient) error`. `Send` must load the current invoice and make exactly one delivery through the manager's default mailer: address the supplied recipient, identify the invoice in the subject, and include its current total in the text body. Register the mailer with the App, preserve caller cancellation, and remain independent from SMTP or provider SDKs. Test the complete delivery locally with `MAIL_DRIVER=log` rather than contacting a real provider.
