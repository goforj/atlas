Add an invoice receipt delivery service with `NewReceiptMailer` and `Send(ctx, invoiceID, recipient) error`. It should load the current invoice and make exactly one valid receipt delivery through the App's configured default mailer: address the supplied recipient, identify the invoice in the subject, and include its current total in the body. Preserve caller cancellation and remain independent from SMTP or provider SDKs. The generated log mailer can exercise the complete delivery locally without contacting a real provider.
