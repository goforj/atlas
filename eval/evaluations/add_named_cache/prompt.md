Configure a named `profiles` memory cache. Add an application-owned `ProfileCache`, constructed by `NewProfileCache(*caches.Manager)`, that selects the generated named accessor and stores a profile value through that cache with `Store(ctx, key, value string) error`. Register its provider with the App Wire services set. Place the boundary in the application package that owns it; do not assume an invoice package. Leave the Project working.
