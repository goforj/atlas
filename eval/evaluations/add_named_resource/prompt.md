Configure a named `reports` queue with two workers. Add an application-owned `ReportDispatcher`, constructed by `NewReportDispatcher(*queues.Manager)`, that selects the generated named accessor and dispatches a report-generation request through that queue with `Dispatch(ctx, invoiceID string) error`. Register its provider with the App Wire services set. Place the boundary in the application package that owns it; do not assume an invoice package. Leave the Project working.
