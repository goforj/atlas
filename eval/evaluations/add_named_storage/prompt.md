Configure a named local `avatars` storage disk rooted at `storage/app/avatars`. Add an application-owned `AvatarStorage`, constructed by `NewAvatarStorage(*storages.Manager)`, that selects the generated named accessor and stores avatar bytes through that disk with `Store(ctx, path string, body []byte) error`. Register its provider with the App Wire services set. Place the boundary in the application package that owns it; do not assume an invoice package. Leave the Project working.
