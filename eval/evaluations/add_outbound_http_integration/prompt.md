Add a typed client for the configured tax-rate service. Its public boundary is `NewClient(baseURL string)` with `Find(ctx, country string) (Rate, error)`. The remote service exposes `GET /rates/{country}`, where `{country}` is URL-path escaped, and returns JSON shaped as `{"country":"US","percent":7.25}`. Keep that remote contract in a focused package, propagate cancellation from callers, return useful error context, keep deployment configuration at the provider boundary, and test it with a local HTTP fixture rather than the external network.
