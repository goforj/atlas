Protect the invoice lookup route with `RequireToken(expectedToken)` in `internal/invoices/middleware.go`. Read `X-Invoice-Token` from each request, return a `401` JSON response with code `unauthorized` when the configured token is empty or the supplied token does not match, and call the next handler when it does match. Resolve `INVOICE_HTTP_TOKEN` once in `provideInvoiceController`, pass it to `NewController(service, token)`, and apply the middleware to the lookup route. Keep the policy out of invoice business behavior and test unconfigured, rejected, and accepted requests.
