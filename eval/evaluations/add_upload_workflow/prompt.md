Add `POST /api/v1/uploads` using the named `uploads` storage resource. In `internal/uploads/service.go`, define `StoreInput` with `Filename`, `ContentType`, and `BodyBase64`, plus `StoredUpload` with `Path`, `Size`, and `ContentType`. `Service.Store(ctx, input)` must validate and decode the base64 body, reduce a name such as `../hello.txt` to a safe stored filename, write through the context-bound `Uploads` storage, and return the stored metadata. Bind the request in `Controller.Store` in `internal/uploads/controller.go`, keep storage behavior in the service, avoid coupling application code to a filesystem or cloud driver, add focused tests, and leave route and Wire registration working.
