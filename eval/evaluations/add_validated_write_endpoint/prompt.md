Add `POST /api/v1/invoices` through `Controller.Store` in `internal/invoices/controller.go`. Bind a `createInvoiceRequest` with `customer_id` and `total_cents`, then pass a `CreateInput` through the existing service and repository; extend `MemoryRepository` with `Create`. Return `400` with code `invalid_payload` for malformed JSON, `422` with code `validation_failed` and field-level errors for decoded input whose customer ID is blank or whose total is not positive, and `201` for a valid request. Trim surrounding whitespace from the customer ID before persistence. Use `validationErrorResponse` for validation failures, add focused tests, and keep the existing lookup endpoint working.
