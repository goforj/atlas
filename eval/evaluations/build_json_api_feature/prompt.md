Add `GET /api/v1/users/:id` to this App. Under `internal/users`, add a `User` with an `ID` and a `NewService()` whose `Find(ctx, id)` returns the existing user with ID `42` and rejects an empty or unknown ID. Add a `Controller` whose `Routes` register `/users/:id`. Keep the controller's HTTP translation thin, return the user as JSON, and return a useful not-found response without prescribing its exact wording. Leave the route and Wire registration working, and keep lookup testable without starting the server.
