Add a named `attachments` storage resource for invoice attachments with `STORAGE_ATTACHMENTS_DRIVER` and `STORAGE_ATTACHMENTS_ROOT`. Under `internal/invoices`, add `Attachment` with a `Path` and `AttachmentService`, constructed by `NewAttachmentService(*storages.Manager)`. Its `Store(ctx, invoiceID, filename, body)` method must write through `manager.Attachments()` and return the stored path; `Read(ctx, path)` must return those bytes from the same resource. Pass caller context to storage, register the service with the App, add focused tests, and keep the storage driver replaceable for shared production storage.
