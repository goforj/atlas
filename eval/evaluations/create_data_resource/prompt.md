Add a new `photos` data resource to this Project. The table must contain an integer `id`, a required `storage_key`, and a required `created_at` timestamp. Apply the schema before deriving the conventional `Photo` model and repository into `internal/photos`, keep the table name domain-native, and add a `FindReady(ctx)` repository method for application code. Preserve generated repository registration and leave the Project working.
