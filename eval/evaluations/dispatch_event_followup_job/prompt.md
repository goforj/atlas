Add a typed `reports:generate` job in `internal/jobs`, with its feature implementation in `internal/reports/generate_job.go`. `GeneratePayload` must carry `UserID` but not email, and `GenerateJobTypeName` must remain `reports:generate`. Construct the feature with `NewService(usersRepository, storage)` and `NewGenerateJob(queueManager, service)`. `GenerateJob.HandleTask` must bind the payload and call `Service.GenerateForUser`, which reloads the current user and writes a `UserReport` containing `UserID` and `Email` to `users/{id}/profile.json`. Expose dispatch behind `ReportQueue` and `GenerateJob.Queue`, and have the user-created notification handler delegate to that queue boundary. Keep context propagation, job and service registration, tests, and queue and storage driver independence working.
