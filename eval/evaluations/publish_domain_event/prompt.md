Add user creation through `Create(ctx, CreateUserInput)`, extending the existing memory repository with save support and returning the created user. Publish a typed `users.created` event containing only the new user's ID in `UserID`; do not put email or other user fields in the event. The event type may be named `UserCreated` or `UserCreatedEvent` and belongs in `internal/events`. Keep publication behind `UserEvents` with a `NewUserEventPublisher` implementation, and publish only after the repository save succeeds. Add a `UserCreatedHandler` and `Subscribers`, constructed with `NewSubscribers(handler)`, whose `Register(ctx, bus)` subscription passes the event's user ID to the handler. Keep the scenario integration API in `internal/users`, `internal/events`, and `internal/notifications` so the supplied behavior check can exercise it; file names within those packages are your choice. Register the subscribers from the lifecycle `Startup` hook, retain the returned subscription, and close it from lifecycle `Shutdown`. Propagate the caller's context throughout, and test that creation reaches the handler without coupling the feature to a concrete event driver.
