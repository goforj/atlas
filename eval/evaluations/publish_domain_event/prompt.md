Add user creation through `Create(ctx, CreateUserInput)`, extending the existing memory repository with save support and returning the created user. Publish a typed `users.created` event containing only the new user's ID (`UserID`); do not include email or other user fields. Keep publication behind `UserEvents` with a `NewUserEventPublisher` implementation, subscribe through the App lifecycle, propagate cancellation, and test that creation reaches the registered handler without coupling the feature to a concrete event driver.
