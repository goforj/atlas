Add user creation through `Create(ctx, CreateUserInput)`, extending the existing memory repository with save support and returning the created user. Publish a typed `users.created` event containing only the new user's ID in `UserID`; do not put email or other user fields in the event. Keep publication behind `UserEvents` with a `NewUserEventPublisher` implementation. Add a `UserCreatedHandler` and `Subscribers`, constructed with `NewSubscribers(handler)`, whose `Register(ctx, bus)` subscription passes the event's user ID to the handler. Register the subscribers from the lifecycle `Startup` hook, propagate cancellation, and test that creation reaches the handler without coupling the feature to a concrete event driver.
