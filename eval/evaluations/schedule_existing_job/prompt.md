Add a `reports:daily` schedule with a 24-hour interval, keeping its schedule boundary in `internal/reports/daily_schedule.go`. Add `DailyTargetRepository.ListDailyReportTargets(ctx)` and a `DailyRunner.Run(ctx)` that asks it for eligible user IDs and calls the existing report queue's `Queue(ctx, userID)` once for each target. Add `DailySchedule.Handle(ctx)` to delegate to the runner, with construction shaped as `NewDailySchedule(NewDailyRunner(repository, reportQueue))`. Keep report generation out of scheduler startup, propagate cancellation, and leave schedule, service, and Wire registration working.
