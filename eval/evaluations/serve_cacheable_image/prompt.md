Add `GET /api/v1/avatars/:id` under `internal/avatars` for the current avatar stored at `users/{id}/avatars/current.png` on the existing named `avatars` resource. Add `Image`, `Service.Find(ctx, id)`, and an image `Digest`; construct the HTTP boundary as `NewController(NewService(manager.Avatars()))` and serve it through `Controller.Show`. A normal request must return the image with an `ETag` and `Cache-Control` policy. When `If-None-Match` matches that validator, return `304` with no body. Pass caller context to storage, add focused tests, and leave the route and Wire registration working.
